
select 'INSERT INTO [types_types] ([attacker_type_id],[defender_type_id],[multiplier])
 VALUES ('+convert(nvarchar(50),id)+',1,'+convert(nvarchar(50),Normal)+')'
from TypesMultiplier

union

select 'INSERT INTO [types_types] ([attacker_type_id],[defender_type_id],[multiplier])
 VALUES ('+convert(nvarchar(50),id)+',2,'+convert(nvarchar(50),Fighting)+')'
from TypesMultiplier

union

select 'INSERT INTO [types_types] ([attacker_type_id],[defender_type_id],[multiplier])
 VALUES ('+convert(nvarchar(50),id)+',3,'+convert(nvarchar(50),Flying)+')'
from TypesMultiplier

union

select 'INSERT INTO [types_types] ([attacker_type_id],[defender_type_id],[multiplier])
 VALUES ('+convert(nvarchar(50),id)+',4,'+convert(nvarchar(50),Poison)+')'
from TypesMultiplier

union

select 'INSERT INTO [types_types] ([attacker_type_id],[defender_type_id],[multiplier])
 VALUES ('+convert(nvarchar(50),id)+',5,'+convert(nvarchar(50),Ground)+')'
from TypesMultiplier

union

select 'INSERT INTO [types_types] ([attacker_type_id],[defender_type_id],[multiplier])
 VALUES ('+convert(nvarchar(50),id)+',6,'+convert(nvarchar(50),Rock)+')'
from TypesMultiplier

union

select 'INSERT INTO [types_types] ([attacker_type_id],[defender_type_id],[multiplier])
 VALUES ('+convert(nvarchar(50),id)+',7,'+convert(nvarchar(50),Bug)+')'
from TypesMultiplier

union

select 'INSERT INTO [types_types] ([attacker_type_id],[defender_type_id],[multiplier])
 VALUES ('+convert(nvarchar(50),id)+',8,'+convert(nvarchar(50),Ghost)+')'
from TypesMultiplier

union

select 'INSERT INTO [types_types] ([attacker_type_id],[defender_type_id],[multiplier])
 VALUES ('+convert(nvarchar(50),id)+',9,'+convert(nvarchar(50),Steel)+')'
from TypesMultiplier

union

select 'INSERT INTO [types_types] ([attacker_type_id],[defender_type_id],[multiplier])
 VALUES ('+convert(nvarchar(50),id)+',10,'+convert(nvarchar(50),Fire)+')'
from TypesMultiplier

union

select 'INSERT INTO [types_types] ([attacker_type_id],[defender_type_id],[multiplier])
 VALUES ('+convert(nvarchar(50),id)+',11,'+convert(nvarchar(50),Water)+')'
from TypesMultiplier

union

select 'INSERT INTO [types_types] ([attacker_type_id],[defender_type_id],[multiplier])
 VALUES ('+convert(nvarchar(50),id)+',12,'+convert(nvarchar(50),Grass)+')'
from TypesMultiplier

union

select 'INSERT INTO [types_types] ([attacker_type_id],[defender_type_id],[multiplier])
 VALUES ('+convert(nvarchar(50),id)+',13,'+convert(nvarchar(50),Electric)+')'
from TypesMultiplier

union

select 'INSERT INTO [types_types] ([attacker_type_id],[defender_type_id],[multiplier])
 VALUES ('+convert(nvarchar(50),id)+',14,'+convert(nvarchar(50),Psychic)+')'
from TypesMultiplier

union

select 'INSERT INTO [types_types] ([attacker_type_id],[defender_type_id],[multiplier])
 VALUES ('+convert(nvarchar(50),id)+',15,'+convert(nvarchar(50),Ice)+')'
from TypesMultiplier

union

select 'INSERT INTO [types_types] ([attacker_type_id],[defender_type_id],[multiplier])
 VALUES ('+convert(nvarchar(50),id)+',16,'+convert(nvarchar(50),Dragon)+')'
from TypesMultiplier

union

select 'INSERT INTO [types_types] ([attacker_type_id],[defender_type_id],[multiplier])
 VALUES ('+convert(nvarchar(50),id)+',17,'+convert(nvarchar(50),Dark)+')'
from TypesMultiplier

union

select 'INSERT INTO [types_types] ([attacker_type_id],[defender_type_id],[multiplier])
 VALUES ('+convert(nvarchar(50),id)+',18,'+convert(nvarchar(50),Fairy)+')'
from TypesMultiplier
